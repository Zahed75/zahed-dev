version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    container_name: zahed_frontend
    restart: unless-stopped
    ports:
      - "4201:80"
    networks:
      - zahed_net

networks:
  zahed_net:
    external: true
